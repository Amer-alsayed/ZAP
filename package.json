{
  "name": "zap-monorepo",
  "version": "1.0.0",
  "description": "Anonymous E2EE Real-time Chat Application",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client --include=dev --legacy-peer-deps && npm run build --prefix client",
    "start": "node server/server.js",
    "test:crypto": "node scripts/test_crypto_security.js",
    "test:integration": "node scripts/test_integration_e2e.js",
    "test": "npm run test:crypto && npm run test:integration",
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev --legacy-peer-deps",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "puppeteer-core": "^25.9.0",
    "puppeteer-screen-recorder": "^3.0.6"
  }
}
